@model List<VolApp.Models.Vol>

<h2>Vols disponibles</h2>

<!-- 🔍 Barre de recherche par ville de départ -->
<input type="text" id="searchInput" class="form-control" placeholder="Rechercher par ville de départ..." style="max-width: 300px; margin-bottom: 15px;" />

<table class="table table-striped" id="volsTable">
    <thead>
        <tr>
            <th>Départ</th>
            <th>Destination</th>
            <th>Date Départ</th>
            <th>Date Arrivée</th>
            <th>Prix</th>
            <th>Places</th>
            <th>Réserver</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var vol in Model)
        {
            <tr>
                <td>@vol.Depart</td>
                <td>@vol.Destination</td>
                <td>@vol.DateDepart.ToString("g")</td>
                <td>@vol.DateArrivee.ToString("g")</td>
                <td>@vol.Prix MAD</td>
                <td>@vol.PlacesDisponibles</td>
                <td>
                    <a href="/Client/Reserver/@vol.Id" class="btn btn-sm btn-success">Réserver</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- 🔁 Script de filtrage -->
<script>
    document.getElementById("searchInput").addEventListener("keyup", function () {
        const filter = this.value.toLowerCase();
        const rows = document.querySelectorAll("#volsTable tbody tr");

        rows.forEach(row => {
            const depart = row.cells[0].textContent.toLowerCase();
            row.style.display = depart.includes(filter) ? "" : "none";
        });
    });
</script>
